<Window x:Class="LidarrCompanion.LogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:LidarrCompanion"
    mc:Ignorable="d"
    Title="Log Window" Height="600" Width="1200"
    Closing="Window_Closing">
<Window.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
</Window.Resources>
<Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top controls -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Label Content="Minimum Severity:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Slider x:Name="sldr_MinSeverity" 
                        Minimum="0" 
                        Maximum="4" 
                        TickFrequency="1" 
                        IsSnapToTickEnabled="True" 
                        Width="200"
                        Value="0"
                        VerticalAlignment="Center"
                        ValueChanged="sldr_MinSeverity_ValueChanged"/>
                <Label x:Name="lbl_SeverityValue" 
                       Content="Verbose" 
                       VerticalAlignment="Center" 
                       Width="80"
                       Margin="10,0,0,0"/>
            </StackPanel>

            <Button Grid.Column="2" 
                    x:Name="btn_ClearLog" 
                    Content="Clear Log" 
                    Click="btn_ClearLog_Click"
                    Width="100"
                    Height="25"/>
        </Grid>

        <!-- Log entries list -->
        <DataGrid Grid.Row="1" 
                  x:Name="dg_LogEntries" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Extended"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding FormattedTimestamp}" Width="90"/>
                <DataGridTextColumn Header="Severity" Binding="{Binding SeverityDisplay}" Width="80"/>
                <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Path / URL" Binding="{Binding FilePath}" Width="250"/>
                <DataGridTemplateColumn Header="" Width="30">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <!-- Folder button for file paths -->
                                <Button Content="📁" 
                                        Width="24" 
                                        Height="24"
                                        ToolTip="Open folder"
                                        Click="btn_OpenFolder_Click"
                                        Tag="{Binding FilePath}"
                                        Padding="0"
                                        FontSize="14">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding HasFilePath}" Value="True"/>
                                                        <Condition Binding="{Binding IsUrl}" Value="False"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                
                                <!-- Copy button for URLs -->
                                <Button Content="📋" 
                                        Width="24" 
                                        Height="24"
                                        ToolTip="Copy URL to clipboard"
                                        Click="btn_CopyUrl_Click"
                                        Tag="{Binding FilePath}"
                                        Padding="0"
                                        FontSize="14">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding HasFilePath}" Value="True"/>
                                                        <Condition Binding="{Binding IsUrl}" Value="True"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            
            <!-- Color code by severity -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Severity}" Value="Critical">
                            <Setter Property="Background" Value="#FFD32F2F"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="High">
                            <Setter Property="Background" Value="#FFFF9800"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Medium">
                            <Setter Property="Background" Value="#FFFDD835"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Low">
                            <Setter Property="Background" Value="#FFE0E0E0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Verbose">
                            <Setter Property="Foreground" Value="Gray"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Status bar -->
        <Border Grid.Row="2" 
                BorderThickness="0,1,0,0" 
                BorderBrush="LightGray" 
                Padding="0,5,0,0" 
                Margin="0,10,0,0">
            <TextBlock x:Name="txt_LogCount" 
                       Text="0 entries" 
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
