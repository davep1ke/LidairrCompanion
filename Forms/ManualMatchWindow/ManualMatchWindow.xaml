<Window x:Class="LidarrCompanion.ManualMatchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Artist" Height="400" Width="678" WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Loaded="Window_Loaded">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txt_Context" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8" />

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <Button x:Name="btn_Reset" Content="Reset" Width="60" Margin="0,0,0,0" Click="btn_Reset_Click" DockPanel.Dock="Right" />
            <Button x:Name="btn_SearchExternal" Content="Search External" Width="100" Margin="0,0,4,0" Click="btn_SearchExternal_Click" DockPanel.Dock="Right"/>
            <Button x:Name="btn_SearchMusicBrainz" Content="Search MusicBrainz" Width="130" Margin="0,0,4,0" Click="btn_SearchMusicBrainz_Click" DockPanel.Dock="Right"/>
            <TextBox x:Name="txt_Search" Margin="0,0,4,0" />
        </DockPanel>

        <Grid Grid.Row="2" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="159.231" />
                <ColumnDefinition Width="20.769"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="list_Artists" Grid.Column="0" SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Artist" Width="200" DisplayMemberBinding="{Binding DisplayName}" />
                        <GridViewColumn Header="Description" Width="260" DisplayMemberBinding="{Binding Description}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <Image x:Name="img_Art" Grid.Column="1" Grid.ColumnSpan="2" />

        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="btn_Ok" Content="OK" Width="80" Margin="0,0,8,0" Click="btn_Ok_Click" IsEnabled="False"/>
            <Button x:Name="btn_Cancel" Content="Cancel" Width="80" Click="btn_Cancel_Click"/>
        </StackPanel>

        <!-- Status text at the very bottom -->
        <TextBlock x:Name="txt_Status" Grid.Row="4" Text="" Margin="0,8,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" />
    </Grid>
</Window>